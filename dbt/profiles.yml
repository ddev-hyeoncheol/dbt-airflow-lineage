data_pipeline:
  target: dev
  outputs:
    dev:
      type: postgres
      host: postgres
      port: 5432
      user: airflow
      pass: airflow
      dbname: airflow
      schema: public
      threads: 4
